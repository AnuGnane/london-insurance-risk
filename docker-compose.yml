services:
  london-insurance-risk:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      # Optional: Mount data directories if you want them accessible externally
      - ./data:/app/data
      - ./reports:/app/reports
